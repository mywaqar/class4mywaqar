import React from 'react';

function Counter(props) {

    return(
            <div>
                <h2>The counter is : {props.count}</h2>
            </div>
        
    );
}

export default Counter;